import { DelayElementDirective } from './delay-element.directive';

describe('DelayElementDirective', () => {
  it('should create an instance', () => {
    const directive = new DelayElementDirective();
    expect(directive).toBeTruthy();
  });
});
